# Cursor Usage Rules for HobbyHash Clean Build

- treat old typo names as `hobbyhash` in all paths, configs, binaries, and docs
- no huge unnecessary requests
- no repo-wide rewrites without approval
- no endless retries
- stop after two repeated failures
- show exact failing command
- no fake pass
- do not touch other coin projects
- no backups unless requested
